import { Menu } from "antd"

const MenuContent = () => {
  return (
    <Menu
      theme="light"
      mode="inline"
      defaultSelectedKeys={[""]}
      selectedKeys={[""]}
      defaultOpenKeys={[""]}
      className="h-max border-r-0"
      items={[]}
    />
  )
}

export default MenuContent
